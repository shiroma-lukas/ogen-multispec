{{ define "middleware"}}
{{- /*gotype: github.com/shiroma-lukas/ogen-multispec/gen.TemplateConfig*/ -}}

{{ template "header" $ }}

// Middleware is middleware type.
type Middleware = middleware.Middleware
{{- end }}
